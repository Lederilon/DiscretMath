package discret;

public class tableHeader {
	
	String[] head ;
	tableHeader(int numEdges ){ 	
	head = new String[numEdges];
	Title.edges = new Edge[numEdges];
	for(int i = 0 ;i < numEdges; i++ )
	 
		 head[i] = "e: "+(i+1);

	 
	
	}
	
}
